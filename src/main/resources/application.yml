logging:
  level:
    root: INFO
#    org.hibernate.SQL: DEBUG
#    org.hibernate.type: TRACE

kafka:
  bootstrapAddress: "http://192.168.1.111:9092"
  topic: "datastream"

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jikanganai
    driver: org.postgresql.Driver
    username: postgres
    password: password
    name: jikanganai
  jpa:
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
        show_sql: true
        format_sql: true